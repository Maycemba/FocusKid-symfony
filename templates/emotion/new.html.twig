{% extends 'base-back.html.twig' %}

{% block title %}Nouvelle émotion{% endblock %}

{% block body %}
<div class="container-fluid">

    <div class="card bg-light-info shadow-none position-relative overflow-hidden mb-4">
        <div class="card-body px-4 py-3">
            <h4 class="fw-semibold mb-8">Créer une émotion</h4>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('emotion/_form.html.twig') }}
            <a href="{{ path('app_emotion_index') }}" class="btn btn-secondary mt-2">← Retour</a>
        </div>
    </div>

</div>
{% endblock %}