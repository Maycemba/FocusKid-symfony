{% extends 'base-back.html.twig' %}

{% block title %}Edit Lecon{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/css/education-forms.css') }}">
{% endblock %}

{% block body %}
    <section class="education-page">
        <div class="education-form-shell">
            <div class="education-form-card">
                <span class="education-form-eyebrow">Gestion pedagogique</span>
                <h1 class="education-form-title">Modifier la lecon</h1>
                <p class="education-form-subtitle">Ajustez le titre, le contenu ou le cours associe a cette lecon.</p>

                {{ include('lecon/_form.html.twig', {'button_label': 'Mettre a jour la lecon'}) }}

                <div class="education-form-actions">
                    <a class="education-btn-secondary" href="{{ path('app_lecon_index') }}">Retour a la liste</a>
                </div>

                {{ include('lecon/_delete_form.html.twig') }}
            </div>
        </div>
    </section>
{% endblock %}
