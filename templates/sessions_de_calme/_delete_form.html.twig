<form method="post" action="{{ path('app_sessions_de_calme_delete', {'id': sessions_de_calme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sessions_de_calme.id) }}">
    <button class="btn">Delete</button>
</form>
