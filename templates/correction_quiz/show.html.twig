{% extends 'base.html.twig' %}

{% block title %}CorrectionQuiz{% endblock %}

{% block body %}
    <h1>CorrectionQuiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_correction</th>
                <td>{{ correction_quiz.idCorrection }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ correction_quiz.contenu }}</td>
            </tr>
            <tr>
                <th>Est_correcte</th>
                <td>{{ correction_quiz.estCorrecte ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_correction_quiz_index') }}">back to list</a>

    <a href="{{ path('app_correction_quiz_edit', {'id_correction': correction_quiz.id_correction}) }}">edit</a>

    {{ include('correction_quiz/_delete_form.html.twig') }}
{% endblock %}
