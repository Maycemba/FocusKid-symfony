<form method="post" action="{{ path('app_reponse_exercice_delete', {'id': reponse_exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_exercice.id) }}">
    <button class="btn">Delete</button>
</form>
